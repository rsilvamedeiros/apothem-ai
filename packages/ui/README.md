# @apothem/ui

APOTHEM design system: shared tokens and primitives. Per `docs/13-design-system`, primitives enter this package only after reuse is demonstrated across apps — start minimal (tokens + `Button`), grow as `apps/web` needs more.

Consumed as workspace source (no build step); Next.js apps must add `@apothem/ui` to `transpilePackages`.
