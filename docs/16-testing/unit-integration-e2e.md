# Unit, Integration and E2E Tests

**Status:** Foundation / Draft  
**Project:** APOTHEM AI  
**Canonical domain:** `apothemai.com.br`

High-value unit tests: authorization policies, autonomy/approval policy, run state transitions, retry/idempotency decisions, schema validation and cost budgets.

Integration tests: PostgreSQL tenant queries, outbox/job coordination, vector retrieval filtering, connection credential abstraction and provider adapter normalization.

E2E MVP journeys: onboarding, agent publish/run, knowledge citation, approval approve/reject, connection revoke, cross-tenant direct-ID denial and run trace inspection.
